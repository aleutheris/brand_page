---
import { withBase } from "../lib/withBase";
import {
  buildPageHref,
  defaultLocale,
  getCurrentLocaleMode,
  localeMeta,
  locales,
  navCopy,
  normalizePath,
  type Locale,
  type PageKey,
} from "../lib/i18n";

interface Props {
  title: string;
  description?: string;
  image?: string;
  locale?: Locale;
  pageKey?: PageKey;
}

const base = import.meta.env.BASE_URL;
const {
  title,
  description = "Automation Architect helping small businesses simplify operations with AI, automation, and practical software solutions.",
  image = withBase(base, "images/og-preview.png"),
  locale = defaultLocale,
  pageKey = "home",
} = Astro.props;

const logoUrl = withBase(base, "images/logo_with_name.png");
const siteUrl = Astro.site ?? new URL("https://aleutheris.com");
const currentPath = Astro.url.pathname;
const { useExplicitEnglish } = getCurrentLocaleMode(currentPath, base);
const navLabels = navCopy[locale];

const homeUrl = buildPageHref(base, "home", locale, {
  forcePrefix: useExplicitEnglish,
});
const projectsUrl = buildPageHref(base, "projects", locale, {
  forcePrefix: useExplicitEnglish,
});
const aboutUrl = buildPageHref(base, "about", locale, {
  forcePrefix: useExplicitEnglish,
});
const reviewsUrl = buildPageHref(base, "reviews", locale, {
  forcePrefix: useExplicitEnglish,
});
const contactUrl = buildPageHref(base, "contact", locale, {
  forcePrefix: useExplicitEnglish,
});

const canonicalUrl = new URL(
  buildPageHref(base, pageKey, locale, {
    forcePrefix: locale !== defaultLocale,
  }),
  siteUrl,
).toString();
const alternateLinks = locales.map((item) => ({
  locale: item,
  href: new URL(
    buildPageHref(base, pageKey, item, { forcePrefix: item !== defaultLocale }),
    siteUrl,
  ).toString(),
}));
const languageLinks = locales.map((item) => ({
  code: item,
  href: buildPageHref(base, pageKey, item, {
    forcePrefix: item !== defaultLocale || useExplicitEnglish,
  }),
  label: localeMeta[item].label,
  name: localeMeta[item].name,
  isCurrent: item === locale,
}));
const ogImageUrl = new URL(image, siteUrl).toString();
const pageTitle =
  pageKey === "home"
    ? `António Eleutério | ${navLabels.siteTagline}`
    : `${title} | António Eleutério`;

function isActivePath(path: string) {
  return normalizePath(currentPath) === normalizePath(path);
}
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index,follow" />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="alternate" hreflang="x-default" href={alternateLinks[0].href} />
    {
      alternateLinks.map((link) => (
        <link rel="alternate" hreflang={link.locale} href={link.href} />
      ))
    }
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="António Eleutério" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content={localeMeta[locale].ogLocale} />
    {
      locales
        .filter((item) => item !== locale)
        .map((item) => (
          <meta
            property="og:locale:alternate"
            content={localeMeta[item].ogLocale}
          />
        ))
    }
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    <link
      rel="icon"
      type="image/png"
      href={withBase(base, "images/logo_square.png")}
    />
    <link
      rel="apple-touch-icon"
      href={withBase(base, "images/logo_square.png")}
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Space+Grotesk:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
    <script
      is:inline
      id="vtag-ai-js"
      async
      src="https://r2.leadsy.ai/tag.js"
      data-pid="aHlLZOTYZ1h29QDb"
      data-version="062024"></script>
    <style is:global>
      :root {
        --bg: #f8fafc;
        --bg-dark: #0f172a;
        --bg-dark-end: #1e293b;
        --surface: #ffffff;
        --ink: #0f172a;
        --ink-light: #f1f5f9;
        --muted: #64748b;
        --muted-light: #94a3b8;
        --brand: #14b8a6;
        --brand-dark: #0d9488;
        --brand-glow: rgba(20, 184, 166, 0.15);
        --stroke: #e2e8f0;
        --radius: 0.75rem;
        --radius-lg: 1rem;
        --radius-xl: 1.25rem;
        --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.05);
        --shadow: 0 4px 16px rgba(0, 0, 0, 0.06);
        --shadow-lg: 0 12px 40px rgba(0, 0, 0, 0.1);
        --transition: 0.2s ease;
        --max-w: min(1120px, 92vw);
      }

      * {
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
        overflow-y: scroll;
        scrollbar-gutter: stable;
      }

      body {
        margin: 0;
        color: var(--ink);
        font-family: "Inter", system-ui, sans-serif;
        background: var(--bg);
        -webkit-font-smoothing: antialiased;
      }

      h1,
      h2,
      h3,
      h4 {
        font-family: "Space Grotesk", system-ui, sans-serif;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      img {
        max-width: 100%;
        height: auto;
      }

      .wrap {
        width: var(--max-w);
        margin-inline: auto;
      }
    </style>
  </head>
  <body>
    <header>
      <nav aria-label="Primary navigation">
        <a href={homeUrl} class="nav-brand">
          <img src={logoUrl} alt="" class="logo" />
        </a>
        <div class="nav-tools">
          <div class="nav-links">
            <a
              href={homeUrl}
              aria-current={isActivePath(homeUrl) ? "page" : undefined}
              >{navLabels.home}</a
            >
            <a
              href={projectsUrl}
              aria-current={isActivePath(projectsUrl) ? "page" : undefined}
              >{navLabels.projects}</a
            >
            <a
              href={aboutUrl}
              aria-current={isActivePath(aboutUrl) ? "page" : undefined}
              >{navLabels.about}</a
            >
            <a
              href={reviewsUrl}
              aria-current={isActivePath(reviewsUrl) ? "page" : undefined}
              >{navLabels.reviews}</a
            >
            <a
              href={contactUrl}
              aria-current={isActivePath(contactUrl) ? "page" : undefined}
              >{navLabels.contact}</a
            >
          </div>
          <div class="language-switcher" aria-label={navLabels.language}>
            {
              languageLinks.map((item) => (
                <a
                  href={item.href}
                  lang={item.code}
                  hreflang={item.code}
                  title={item.name}
                  aria-current={item.isCurrent ? "true" : undefined}
                >
                  {item.label}
                </a>
              ))
            }
          </div>
        </div>
        <button
          class="hamburger"
          aria-label="Toggle navigation menu"
          aria-expanded="false"
          type="button"
        >
          <span class="hamburger-line"></span>
          <span class="hamburger-line"></span>
          <span class="hamburger-line"></span>
        </button>
      </nav>
      <div class="mobile-menu-overlay" aria-hidden="true">
        <div class="mobile-menu">
          <div class="mobile-nav-links">
            <a
              href={homeUrl}
              aria-current={isActivePath(homeUrl) ? "page" : undefined}
              >{navLabels.home}</a
            >
            <a
              href={projectsUrl}
              aria-current={isActivePath(projectsUrl) ? "page" : undefined}
              >{navLabels.projects}</a
            >
            <a
              href={aboutUrl}
              aria-current={isActivePath(aboutUrl) ? "page" : undefined}
              >{navLabels.about}</a
            >
            <a
              href={reviewsUrl}
              aria-current={isActivePath(reviewsUrl) ? "page" : undefined}
              >{navLabels.reviews}</a
            >
            <a
              href={contactUrl}
              aria-current={isActivePath(contactUrl) ? "page" : undefined}
              >{navLabels.contact}</a
            >
          </div>
          <div class="mobile-language-switcher" aria-label={navLabels.language}>
            {
              languageLinks.map((item) => (
                <a
                  href={item.href}
                  lang={item.code}
                  hreflang={item.code}
                  title={item.name}
                  aria-current={item.isCurrent ? "true" : undefined}
                >
                  {item.label}
                </a>
              ))
            }
          </div>
        </div>
      </div>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <div class="wrap">
        <p>&copy; {new Date().getFullYear()} &middot; {navLabels.footer}</p>
      </div>
    </footer>
    <script>
      // Mobile menu toggle functionality
      document.addEventListener("DOMContentLoaded", function () {
        const hamburger = document.querySelector(".hamburger");
        const overlay = document.querySelector(".mobile-menu-overlay");
        const body = document.body;

        if (hamburger && overlay) {
          hamburger.addEventListener("click", function () {
            const isExpanded =
              hamburger.getAttribute("aria-expanded") === "true";
            hamburger.setAttribute("aria-expanded", (!isExpanded).toString());
            overlay.setAttribute("aria-hidden", isExpanded.toString());
            body.classList.toggle("mobile-menu-open", !isExpanded);
          });

          // Close menu when clicking on overlay
          overlay.addEventListener("click", function (e) {
            if (e.target === overlay) {
              hamburger.setAttribute("aria-expanded", "false");
              overlay.setAttribute("aria-hidden", "true");
              body.classList.remove("mobile-menu-open");
            }
          });

          // Close menu when clicking on a link
          overlay.querySelectorAll("a").forEach((link) => {
            link.addEventListener("click", function () {
              hamburger.setAttribute("aria-expanded", "false");
              overlay.setAttribute("aria-hidden", "true");
              body.classList.remove("mobile-menu-open");
            });
          });
        }
      });
    </script>
    <style>
      header {
        position: sticky;
        top: 0;
        z-index: 50;
        backdrop-filter: blur(14px) saturate(1.6);
        -webkit-backdrop-filter: blur(14px) saturate(1.6);
        background: rgba(248, 250, 252, 0.82);
        border-bottom: 1px solid var(--stroke);
      }

      nav {
        display: flex;
        align-items: center;
        width: var(--max-w);
        margin: 0 auto;
        padding: 0.7rem 0;
        gap: 1.25rem;
      }

      .nav-brand {
        display: flex;
        flex-shrink: 0;
      }

      .logo {
        height: 2rem;
        width: auto;
      }

      .nav-tools {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-left: auto;
        flex-wrap: wrap;
        justify-content: flex-end;
      }

      .nav-links {
        display: flex;
        gap: 0.15rem;
      }

      .nav-links a {
        font-family: "Space Grotesk", system-ui, sans-serif;
        font-weight: 600;
        font-size: 0.9rem;
        padding: 0.45rem 0.7rem;
        border-radius: var(--radius);
        transition:
          background var(--transition),
          color var(--transition);
      }

      .nav-links a:hover,
      .nav-links a:focus-visible,
      .nav-links a[aria-current="page"] {
        background: var(--brand-glow);
        color: var(--brand-dark);
        outline: none;
      }

      .nav-links a[aria-current="page"] {
        box-shadow: inset 0 0 0 1px rgba(20, 184, 166, 0.28);
      }

      .language-switcher {
        display: inline-flex;
        align-items: center;
        gap: 0.2rem;
        padding: 0.2rem;
        border: 1px solid var(--stroke);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.72);
      }

      .language-switcher a {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 2rem;
        min-height: 1.9rem;
        padding: 0 0.45rem;
        border-radius: 999px;
        font-size: 0.74rem;
        font-weight: 700;
        color: var(--muted);
        transition:
          background var(--transition),
          color var(--transition);
      }

      .language-switcher a:hover,
      .language-switcher a:focus-visible,
      .language-switcher a[aria-current="true"] {
        background: var(--brand-glow);
        color: var(--brand-dark);
        outline: none;
      }

      main {
        min-height: 60vh;
      }

      footer {
        margin-top: 3rem;
        border-top: 1px solid var(--stroke);
        padding: 1.5rem 0;
      }

      footer p {
        margin: 0;
        text-align: center;
        color: var(--muted);
        font-size: 0.84rem;
      }

      @media (max-width: 640px) {
        nav {
          gap: 0.5rem;
        }

        .logo {
          height: 1.6rem;
        }

        .nav-tools {
          gap: 0.45rem;
        }

        .nav-links {
          gap: 0;
          flex-wrap: wrap;
          justify-content: flex-end;
        }

        .nav-links a {
          padding: 0.35rem 0.45rem;
          font-size: 0.84rem;
        }

        .language-switcher {
          width: 100%;
          justify-content: flex-end;
        }
      }

      /* Mobile menu styles */
      .hamburger {
        display: none;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        width: 2.5rem;
        height: 2.5rem;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5rem;
        border-radius: var(--radius);
        transition: background var(--transition);
      }

      .hamburger:hover,
      .hamburger:focus-visible {
        background: var(--brand-glow);
        outline: none;
      }

      .hamburger-line {
        display: block;
        width: 1.25rem;
        height: 2px;
        background: var(--ink);
        margin: 2px 0;
        transition: all 0.3s ease;
        border-radius: 1px;
      }

      .mobile-menu-open .hamburger-line:nth-child(1) {
        transform: rotate(45deg) translate(5px, 5px);
      }

      .mobile-menu-open .hamburger-line:nth-child(2) {
        opacity: 0;
      }

      .mobile-menu-open .hamburger-line:nth-child(3) {
        transform: rotate(-45deg) translate(7px, -6px);
      }

      .mobile-menu-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: rgba(15, 23, 42, 0.8);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        z-index: 40;
        opacity: 0;
        visibility: hidden;
        transition:
          opacity 0.3s ease,
          visibility 0.3s ease;
      }

      .mobile-menu-overlay[aria-hidden="false"] {
        opacity: 1;
        visibility: visible;
      }

      .mobile-menu {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: var(--surface);
        border-radius: var(--radius-xl);
        padding: 2rem;
        width: 90%;
        max-width: 320px;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--stroke);
      }

      .mobile-nav-links {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        margin-bottom: 1.5rem;
      }

      .mobile-nav-links a {
        display: block;
        padding: 0.75rem 1rem;
        border-radius: var(--radius);
        font-family: "Space Grotesk", system-ui, sans-serif;
        font-weight: 600;
        font-size: 1rem;
        text-align: center;
        transition:
          background var(--transition),
          color var(--transition);
        border: 1px solid transparent;
      }

      .mobile-nav-links a:hover,
      .mobile-nav-links a:focus-visible,
      .mobile-nav-links a[aria-current="page"] {
        background: var(--brand-glow);
        color: var(--brand-dark);
        border-color: rgba(20, 184, 166, 0.28);
        outline: none;
      }

      .mobile-language-switcher {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.2rem;
        padding: 0.5rem;
        border: 1px solid var(--stroke);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.9);
      }

      .mobile-language-switcher a {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 2.5rem;
        min-height: 2.2rem;
        padding: 0 0.6rem;
        border-radius: 999px;
        font-size: 0.8rem;
        font-weight: 700;
        color: var(--muted);
        transition:
          background var(--transition),
          color var(--transition);
      }

      .mobile-language-switcher a:hover,
      .mobile-language-switcher a:focus-visible,
      .mobile-language-switcher a[aria-current="true"] {
        background: var(--brand-glow);
        color: var(--brand-dark);
        outline: none;
      }

      /* Mobile responsive behavior */
      @media (max-width: 767px) {
        .nav-tools {
          display: none;
        }

        .hamburger {
          display: flex;
        }

        .nav-links {
          display: none;
        }

        .language-switcher {
          display: none;
        }
      }

      /* Prevent body scroll when mobile menu is open */
      .mobile-menu-open {
        overflow: hidden;
      }
    </style>
  </body>
</html>
